// const cuenta = 10 
// let mensaje;

// if (cuenta < 0)
//     mensaje = "no tienes saldo"
// else
//     mensaje = "tiene saldo"

// document.write(mensaje);

const cuenta = -10;
const mensaje = cuenta < 0 ? "no tiene saldo segun ternario" : "tiene saldo segun ternario";

document.write(mensaje);

